{
  "name": "allquery",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build --workspace=@allquery/core && npm run build --workspace=@allquery/express && npm run build --workspace=@allquery/nest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.2",
    "@types/node": "^20.6.0",
    "@types/query-string": "^6.3.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
